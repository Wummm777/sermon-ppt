# 讲道稿转PPT · 使用说明

## 界面模式（推荐）

双击 `sermonPPT.exe`，主界面分三步：

1. **选择讲道稿**：点「浏览」选 Word（.docx）或 TXT 文件
2. **选择模式**：
   - **规则匹配（推荐）**：程序自动识别题目/大点/小点/经文，**无需 AI**，点「生成 PPT」即完成
   - **AI 加工**：切到此模式后按界面两步走（提取复制 → 贴给 DeepSeek → 回答贴回来 → 生成）
3. **生成 PPT**：选保存位置，完成（可直接打开）

> 拖拽快捷方式：把 docx/txt 拖到 exe 图标上 = 规则模式直接转换。

## 两条工作路线，按需选择

### 路线 A：逐字稿 → DeepSeek 提炼 → 规则匹配（快速出片）

适合：手里是**逐字稿**（一大篇讲道文字），想快速得到 PPT。

1. 打开 `AI逐字稿提炼提示词.txt`，全文复制给 DeepSeek
2. 再把逐字稿内容发给 DeepSeek，它会输出一份结构化大纲（纯文本）
3. 把大纲保存为 `.txt` 文件
4. 双击 `sermonPPT.exe` → 选这个 txt → **规则匹配** → 生成

> 注意：此路线的输出是纯文本，PPT 里没有 Word 样式（粗体/颜色），全部按「排版设置」的预设渲染。

### 路线 B：已排版 Word 讲稿 → AI 加工（保留样式重点）

适合：Word 里已经做好大纲+经文，且用**颜色/粗体/下划线**标了重点，想原样保留。

1. 主界面切到「AI 加工」模式
2. 选好 Word 文件，点「提取并复制」（若剪贴板失败，框内文本可手动全选复制）
3. 点「复制提示词」，连同讲稿文本一起发给 DeepSeek
4. 复制 AI 回答贴回来 → 生成 → Word 里划的重点原样进 PPT

## 规则匹配的识别规则

| 行的特征 | 识别为 |
|---|---|
| 含「题目：」/「Title:」 | 题目 |
| 含「经文：」/「Scripture:」 | 主题经文出处 |
| 行首「一、」「1.」编号，或含「绪论」「Introduction」 | 大点 |
| 行首「1）」「（1）」 | 小点 |
| 「【太6:13】」开头 | 经文正文（出处与正文同行，整体显示） |

- 中文行后紧跟英文行自动配对；中英不必都存在
- 无法识别的行会**报错**并列出位置，不会静默丢弃
- Word 里的粗体/颜色/下划线直接保留进 PPT（docx 输入时）

## AI 加工模式（备用）

当格式特殊、规则识别报错时使用：

1. 点「提取并复制」→ 程序把讲道稿变成带样式标记的文本并复制到剪贴板
2. 点「复制提示词」→ 把提示词粘到 DeepSeek，再粘讲道稿文本，发送
3. 复制 AI 的回答，粘回程序下方的文本框
4. 点「生成 PPT」

样式标记说明：`**粗体**`、`__下划线__`、`{c:RRGGBB}颜色{/c}`

## 排版设置

主界面点「排版设置…」，分四页：

- **字号**：左侧各元素 + 提纲统一字号
- **颜色**：题目/大点/小点/英文经文/出处/高亮（可输入 hex 或点「选色」取色器）
- **字体与提纲**：中英文字体、行距、段后距、边距、列间隙、粗体开关
- **背景图**：选择图片（自动收进程序同目录 `background/` 文件夹，**相同内容的图只存一份**，可放心从文件夹里反复选）、不透明度滑杆（100%=原图，越低文字越清晰）、「打开背景文件夹」一键管理已收藏的图

设置保存到 exe 同目录 `config.json`；「恢复默认」一键还原。颜色支持 `#RRGGBB` 或 `RRGGBB`。

## 常见问题

- **规则模式报“无法识别”**：检查该行开头是否符合上表特征；或改用 AI 模式
- **背景图不显示**：确认图片在 `background/` 文件夹里且 config 里文件名正确
- **程序无响应/异常**：看同目录 `崩溃日志.txt` 或 `转换错误报告.txt`

## 文件清单

| 文件 | 用途 |
|---|---|
| `sermonPPT.exe` | 主程序（界面 + 转换） |
| `config.json` | 排版设置（界面会读写它） |
| `AI提示词模板.md` | 路线 B 的提示词（给已排版 Word 加 tag） |
| `AI逐字稿提炼提示词.txt` | 路线 A 的提示词（逐字稿提炼成大纲） |
| `background\` | 背景图片文件夹 |
