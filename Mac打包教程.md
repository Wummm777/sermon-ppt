# 讲道稿转PPT · Mac 版打包教程（照着一步一步做就行）

> 这份教程写给第一次用终端的朋友。
> 每一步都有"会看到什么"，跟着走就行，哪里卡住了就把那一步的截图发出去问，不要自己琢磨。
> 全程大约 15~20 分钟，其中大部分时间在等安装。

---

## 开始前的准备

确认这几样东西：

1. 你的电脑是苹果电脑（iMac / MacBook 都行）
2. 电脑能正常上网
3. 你知道自己的**开机密码**（第 1 步会用到）

---

## 第 1 步：安装 Homebrew（Mac 的软件安装工具，约 5 分钟）

1. 打开"终端"这个程序：
   - 同时按键盘上的 `Command(⌘) + 空格`，会弹出一个搜索框
   - 输入 `terminal` 或 `终端`
   - 按回车，会弹出一个黑底或白底的窗口，里面有一行字等待你输入——这就是"终端"，接下来所有命令都粘贴到这个窗口里

2. 复制下面**整段**（从 `/bin` 开始到最后的 `"` 结束，一次全选复制）：

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

3. 粘贴到终端窗口里（在窗口里按 `Command(⌘) + V`），按**回车**

4. 接下来会发生这些事（都是正常的）：
   - 先问你要输**开机密码** → 输入时**屏幕上不会显示任何字符**（连星号都没有），这是安全设计，**盲打输入后直接回车**即可
   - 然后列出要安装的东西 → 显示 `Press RETURN/ENTER to continue` → **再按一次回车**
   - 然后开始下载安装，**等 3~10 分钟**，期间它会滚动很多英文，不要管，等它停下

5. 安装完成的标志：最后一行出现
   `==> Installation successful!` 或 `Next steps:`

5. 安装完成的标志：最后一行出现
   `==> Installation successful` 或 `==> Installation successful!`
   （后面还可能跟着 `==> Next steps:` 的一段说明——这正是第 6 点要处理的）

6. ⚠️ **很重要——"登记" brew 进系统（很多电脑都有这一步）**

   安装成功的下面，如果出现了 **`==> Next steps:`** 开头的一段，里面通常有两行以 **`echo`** 或 **`eval`** 开头的命令，长得像这样（**你的电脑上的具体内容和下面可能不完全一样，以你自己屏幕上显示的为准**）：

   ```
   echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
   eval "$(/opt/homebrew/bin/brew shellenv)"
   ```

   怎么做：
   - 把屏幕上**第一行**（echo 开头的那行）完整复制 → 粘贴到终端 → 回车
   - 再把**第二行**（eval 开头的那行）完整复制 → 粘贴 → 回车
   - **一行一行做**，别两行一起粘（可能粘连出错）
   - 执行完**没有任何显示就是对的**（这两行命令本来就不产生输出）

   怎么确认成功：再输入这一行并回车——

   ```
   brew --version
   ```

   如果显示 `Homebrew 4.x.x` 之类的版本号 → ✅ 登记成功，继续第 2 步
   如果显示 `command not found: brew` → ❌ 说明刚才的两行没执行对，重新复制粘贴那两行再来一次

   （如果安装完**没有出现** `==> Next steps:` 这一段，说明你的电脑不需要登记，直接继续第 2 步即可。）

---

## 第 2 步：安装 Python

接着在同一个终端窗口，粘贴下面这行，回车：

```
brew install python@3.11
```

- 会下载几分钟，等它跑完
- 如果它提示什么 "already installed"（已安装），不用管，继续下一步

---

## 第 3 步：下载程序代码

1. 用浏览器（Safari 或 Chrome 都行）打开这个网页：

   **https://github.com/Wummm777/sermon-ppt**

2. 点页面上绿色的 **`<> Code`** 按钮（代码区右上角）

3. 在弹出的小窗里点 **Download ZIP**（下载压缩包）

4. 打开"访达"（Finder， Dock 栏上那个笑脸图标）→ 左侧点"下载"（Downloads）→ 找到 `sermon-ppt-main.zip` → **双击它**解压

5. 解压后得到一个文件夹 **`sermon-ppt-main`**，记住它的位置（就在"下载"里）

---

## 第 4 步：一键打包（复制 3 行命令）

1. 回到终端窗口

2. 先复制下面**第一行**，粘贴，回车：

   ```
   cd ~/Downloads/sermon-ppt-main
   ```

   （这一步是"进入代码文件夹"，没有任何提示就是成功了）

3. 再复制下面**第二行**，粘贴，回车：

   ```
   pip3 install python-docx python-pptx pillow pyinstaller
   ```

   （这一步在下载打包工具，约 1~2 分钟；如果出现大量黄色 Warning 无所谓，只要最后没有红色 Error 就行）

4. 最后复制下面**第三行**，粘贴，回车：

   ```
   MACOSX_DEPLOYMENT_TARGET=10.15 pyinstaller --onefile --windowed --name 讲道稿转PPT app.py
   ```

   （这一步是真正打包，约 2~5 分钟，会滚动很多英文）

5. **成功的标志**：最后几行出现

   ```
   Building EXE ... completed successfully.
   ```

   （或 `Building APP ... completed successfully.`）——看到这个就全部完成了！

---

## 第 5 步：找到打包好的程序

1. 打开访达 → 左侧"下载" → 进入 **sermon-ppt-main** 文件夹
2. 里面有个 **dist** 文件夹，进入
3. 里面就有 **讲道稿转PPT.app** —— 这就是成品！

**双击它就能用**（自己电脑打包的，不会有"无法验证开发者"的拦截）

建议：把它拖到访达左侧的"应用程序"里，以后像其他软件一样使用。

---

## 第 6 步：补齐程序的"配套文件"

光有 app 还不够，程序还需要 4 个小文件放在**同一个文件夹**里才能完整工作：

| 文件 | 作用 |
|---|---|
| `config.json` | 排版设置（字号/颜色等） |
| `AI提示词模板.md` | AI 模式的提示词 |
| `AI逐字稿提炼提示词.txt` | 逐字稿提炼模式的提示词 |
| `使用说明.md` | 完整使用教程 |

这些文件让发消息给你的人发给你（或按对方指示获取），收到后**全部拖进"应用程序"文件夹里、和 讲道稿转PPT.app 放在一起**即可。

---

## 以后怎么用（日常流程）

1. 双击 讲道稿转PPT.app
2. 界面上选讲道稿文件 → 选模式 → 点"生成 PPT"
3. 详细功能看"使用说明.md"

---

## 常见问题（出问题了看这里）

**Q：第 1 步输密码时屏幕没反应？**
正常。Mac 的终端输密码就是什么都不显示，盲打输入后直接回车。

**Q：第 1 步装到一半弹出"需要安装命令行开发者工具"？**
点"安装"，等它装完（约5分钟）后重新执行那条 brew 安装命令。

**Q：第 2 步装 Python 时报 `command not found: brew`？**
说明第 1 步最后的"登记"没做成功，回到第 1 步第 6 点，重新执行那两行 echo/eval 命令，再用 `brew --version` 验证。

**Q：第 4 步第 3 行报错 `command not found: pyinstaller`？**
说明第 4 步第 2 行（pip3 那行）没装成功，把它重新执行一遍再看。

**Q：第 4 步报错一大段红字？**
不要自己研究，把终端**最后 10 行左右**截图发出去，能解决。

**Q：打包完了，dist 文件夹里没找到 app？**
看一眼终端最后是不是 `completed successfully`，是的话 app 一定在 dist 里；访达里按 `Command(⌘) + F` 搜"讲道稿"。

**Q：想重新打包一次（比如代码更新了）？**
只需要重新执行第 4 步的第 4 行命令即可（前面几步装过的都不用重装）。

---

## 附：把打包结果分享给别人

`dist/讲道稿转PPT.app` 可以直接发给其他苹果电脑用户（微信/网盘都行）。
对方收到后：**右键点它 → 打开**（第一次要这样，之后双击就行）。
对方也需要把上面那 4 个配套文件和 app 放同一个文件夹。